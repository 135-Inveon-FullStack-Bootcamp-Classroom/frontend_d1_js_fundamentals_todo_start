// function getFullName(name, surname) {
//   return name + " " + surname;
// }

const getFullName = (name, surname) => name + " " + surname;

// const getFullName = (name, surname) => {
//     name = name + name;
//     return name + " " + surname;
//   };

console.log(getFullName("murat", "türkay"));
